// VoltXAmpere v2 — uygulama giriş noktası.
// Burada yalnızca kök bileşeni import ediyoruz; custom element register'ı
// `app-root.ts` içindeki @customElement decorator'ı halleder.
// Sprint 0.1 kapsamında başka bir bootstrap mantığı yok.
import './design/tokens.css';
import './app-root.ts';
